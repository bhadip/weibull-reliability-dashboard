version: '3.8'
services:
  bcore-dashboard:
    build: .
    ports:
      - "100.75.220.84:8501:8501"  # Bound to Tailscale, just like your MT4 server
    volumes:
      - .:/app
      - ./.streamlit:/app/.streamlit
    restart: unless-stopped
